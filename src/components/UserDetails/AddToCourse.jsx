import React from 'react';
import { courseForId } from '../../Utils/';

const AddToCourse = ({ queriedUser, allCourses }) => (
  <div>AddToCourse: TODO before rollout</div>
);

export default AddToCourse;
